"""Marshmallow schemas: request and response shapes, one file per feature.

They also document the API: flask-smorest builds the OpenAPI spec from them
(Swagger UI at /api/docs), which the frontend uses as its field reference.
"""
