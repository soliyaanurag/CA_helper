"""In-app notifications (owner: Member B). Empty in Phase 0.

Planned (INF-05): Notification model, `notify(user_id, ...)` service used by
other modules (it also sends the email), and the notification tray API.
"""
