"""Outgoing email (owner: Member B). Empty in Phase 0.

Planned (INF-05): `send_email(to, subject, body)` over SMTP using the MAIL_*
settings. In development every email lands in Mailpit (http://localhost:8025).
"""
