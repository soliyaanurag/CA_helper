# Runtime dependencies for the backend and worker.
# Used by: the conda env (via requirements-dev.txt) and CI.
# Pin exact versions. Add a package in the PR of the feature that first imports it
# (e.g. PyMuPDF with OCR, google-genai with the assistant), then run `make env-update`
# and mention it in the PR.

# --- Web framework and API ---
Flask==3.1.3
flask-smorest==0.47.0
marshmallow==4.3.1
flask-cors==6.0.5
Flask-Limiter==4.1.1
Flask-JWT-Extended==4.7.4
python-dotenv==1.2.3

# --- Database ---
Flask-SQLAlchemy==3.1.1
# SQLAlchemy is held at 2.0.x: Flask-SQLAlchemy 3.1.1 predates SQLAlchemy 2.1.
SQLAlchemy==2.0.54
Flask-Migrate==4.1.0
psycopg[binary]==3.3.6

# --- Security ---
argon2-cffi==25.1.0

# --- Background jobs (worker process only) ---
APScheduler==3.11.3
