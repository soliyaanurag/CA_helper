# Runtime dependencies for the backend and worker.
# Used by: the conda env (via requirements-dev.txt), backend/Dockerfile, and CI.
# Pin exact versions. After editing, run `make env-update` and mention the change in your PR.

# --- Web framework and API ---
Flask==3.1.3
flask-smorest==0.47.0
marshmallow==4.3.1
flask-cors==6.0.5
Flask-Limiter==4.1.1
Flask-JWT-Extended==4.7.4
python-dotenv==1.2.3

# --- Database ---
Flask-SQLAlchemy==3.1.1
# SQLAlchemy is held at 2.0.x: Flask-SQLAlchemy 3.1.1 predates SQLAlchemy 2.1.
SQLAlchemy==2.0.54
Flask-Migrate==4.1.0
psycopg[binary]==3.3.6
pgvector==0.5.0

# --- Security ---
argon2-cffi==25.1.0
cryptography==50.0.1

# --- Background jobs (worker process only) ---
APScheduler==3.11.3

# --- AI (Gemini; always via app/utils/gemini_client.py) ---
google-genai==2.25.0

# --- OCR and document extraction (local only) ---
pytesseract==0.3.13
PyMuPDF==1.28.2
pdfplumber==0.11.10
opencv-python-headless==5.0.0.93

# --- Scraping (regulatory monitor) ---
requests==2.34.2
beautifulsoup4==4.15.0
feedparser==6.0.14

# --- Production server (used only inside the Docker image) ---
gunicorn==26.2.0
