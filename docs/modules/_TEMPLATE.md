# <module>: <one-line summary>

<!-- Copy this file to docs/modules/<module>.md for a new module. Keep all eight sections; write "None yet." when
     a section is empty. Update the doc in every PR that changes the module, so it always matches the code. -->

## Purpose
What this module does for users, in 2-4 sentences.

## What exists now
What is actually in the code today (backend, frontend, tests, seed data) and where it lives. Only facts; plans
go in the sections below, marked "Planned".

## Tables
- `table_name`: what one row means (details in docs/DATA_MODEL.md)

## Endpoints
- `GET /api/v1/<module>/...`: what it returns

## Service functions other modules call
- `function_name(args) -> result`: what it guarantees

## Depends on
Other modules/core pieces this module calls.

## Contracts (don't change without telling the team)
Status values, codes, response shapes or behaviour other modules depend on. A PR that changes one says so at
the top of its description.

## Known issues
- ...
