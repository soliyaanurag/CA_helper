/**
 * API calls for the onboarding module: TanStack Query hooks around the typed
 * client, e.g.
 *
 *   export function useThing(id: number) {
 *     return useQuery({ queryKey: ["onboarding", "thing", id], queryFn: ... api.GET(...) });
 *   }
 *
 * Nothing here yet.
 */
export {};
