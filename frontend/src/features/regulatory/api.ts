/**
 * API calls for the regulatory module: TanStack Query hooks around the typed
 * client, e.g.
 *
 *   export function useThing(id: number) {
 *     return useQuery({ queryKey: ["regulatory", "thing", id], queryFn: ... api.GET(...) });
 *   }
 *
 * Nothing here yet.
 */
export {};
