/**
 * API calls for the assistant module: TanStack Query hooks around the typed
 * client, e.g.
 *
 *   export function useThing(id: number) {
 *     return useQuery({ queryKey: ["assistant", "thing", id], queryFn: ... api.GET(...) });
 *   }
 *
 * Nothing here yet.
 */
export {};
