/**
 * API calls for the marketplace module: TanStack Query hooks around the typed
 * client, e.g.
 *
 *   export function useThing(id: number) {
 *     return useQuery({ queryKey: ["marketplace", "thing", id], queryFn: ... api.GET(...) });
 *   }
 *
 * Nothing here yet.
 */
export {};
