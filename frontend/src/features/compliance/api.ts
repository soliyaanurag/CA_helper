/**
 * API calls for the compliance module: TanStack Query hooks around the typed
 * client, e.g.
 *
 *   export function useThing(id: number) {
 *     return useQuery({ queryKey: ["compliance", "thing", id], queryFn: ... api.GET(...) });
 *   }
 *
 * Nothing here yet.
 */
export {};
